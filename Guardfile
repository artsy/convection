# frozen_string_literal: true

guard 'livereload', port: '5003', grace_period: 0.5 do
  watch(%r{app/assets/.+})
  watch(%r{app/controllers/.+})
  watch(%r{app/helpers/.+})
  watch(%r{app/views/.+})
end
