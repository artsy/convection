<p>We have received the following submission from: <%= @user.user_name %> (<%= mail_to(@user.user_email) %>)</p>

<%= link_to('View Submission In Admin', "#{Convection.config.convection_url}/admin/submissions/#{@submission.id}") %>

<%= render 'shared/email/submission' %>
